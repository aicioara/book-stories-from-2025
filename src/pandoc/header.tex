% Fonts
\usepackage{fontspec}
\directlua{luaotfload.add_fallback("emojifallback", {"Apple Color Emoji:mode=harf;"})}
\setmainfont{"HelveticaNeue"}[RawFeature={fallback=emojifallback}]
\setsansfont{"HelveticaNeue"}[RawFeature={fallback=emojifallback}]
\setmonofont{"Menlo"}[Scale=MatchLowercase,RawFeature={fallback=emojifallback}]
\setmathrm{"HelveticaNeue"}[RawFeature={fallback=emojifallback}]
\setmathsf{"HelveticaNeue"}[RawFeature={fallback=emojifallback}]
\setmathtt{"Menlo"}[Scale=MatchLowercase,RawFeature={fallback=emojifallback}]

% Page Breaks
\usepackage{microtype}
\usepackage[hyphens]{url}
\usepackage{hyperref}
\usepackage{fvextra}
\DefineVerbatimEnvironment{Highlighting}{Verbatim}{breaklines, breakanywhere, commandchars=\\\{\}}
\sloppy
\emergencystretch=3em

% Headers & Footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}               % clear header/footer
\fancyfoot[C]{\thepage}  % page number bottom-center
\renewcommand{\headrulewidth}{0pt}

% Add two empty pages for dedications
\AtBeginDocument{%
    \thispagestyle{empty}\null\newpage
    \thispagestyle{empty}\null\newpage
    \setcounter{page}{1}
}

% Title
\usepackage{titling}
\pretitle{\begin{center}\Huge\bfseries}
\posttitle{\par\end{center}\vspace{3em}}

% Pages
\raggedbottom

% Move page numbers lower to avoid overlaps with images
\setlength{\footskip}{35pt}

% Do not add Figure X prefix to captions
\usepackage{caption}
\captionsetup[figure]{labelformat=empty}

% Center figures
\usepackage{etoolbox}
\makeatletter
\preto{\@floatboxreset}{\centering}
\makeatother

% Bullet points should always be round
\renewcommand{\labelitemi}{\textbullet}
\renewcommand{\labelitemii}{\textbullet}
\renewcommand{\labelitemiii}{\textbullet}
\renewcommand{\labelitemiv}{\textbullet}

% No double space after periods
\frenchspacing

% Block Quotes
\usepackage{xcolor}
\usepackage{framed}

\definecolor{quoteblue}{RGB}{52,109,187}

\renewenvironment{quote}
  {%
    \vspace{1.0\baselineskip} % space before
    \def\FrameCommand{%
      {\color{quoteblue}\vrule width 2pt}\hspace{0.8em}%
    }%
    \MakeFramed{\advance\hsize-\width\FrameRestore}%
    \noindent\ignorespaces
  }
  {%
    \endMakeFramed
    \vspace{0.5\baselineskip} % space after
  }
